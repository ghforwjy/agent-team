# 基因优胜劣汰机制

> Evolver 的"自然选择" - 如何筛选和优化基因库

---

## 核心概念

### 什么是优胜劣汰？

**优胜劣汰**是 Evolver 系统的核心进化机制，类似于生物进化中的自然选择。它确保：
- ✅ **优质基因**被保留、强化和共享
- ❌ **劣质基因**被抑制、封禁或淘汰

**一句话理解**：让成功的解决方案更容易被选中，让失败的方案逐渐淡出。

---

## 类比理解

### 生活类比：企业人才管理

| 机制 | 企业场景 | Evolver 对应 |
|------|---------|-------------|
| **信号匹配** | 根据岗位需求筛选简历 | 根据问题特征匹配合适的基因 |
| **试用期考核** | 新员工3个月试用期 | 基因的验证和金丝雀检查 |
| **绩效评估** | KPI考核优秀员工晋升 | 表观遗传标记增强成功基因 |
| **末位淘汰** | 连续低绩效员工离职 | 失败2次的基因进入封禁列表 |
| **能力沉淀** | 优秀经验写入SOP手册 | 成功的胶囊发布到Hub共享 |

### 生物类比：自然选择

```
环境压力（错误信号）
    ↓
基因变异（Mutation）
    ↓
自然选择（Selector评分）
    ↓
适者生存（优质基因保留）
    ↓
遗传强化（表观遗传标记）
```

---

## 优胜劣汰的6层机制

### 第1层：信号匹配筛选（Selector）

**位置**：[`selector.js`](../../evolver/src/gep/selector.js)

```javascript
// 评分算法
function scoreGene(gene, signals) {
  let score = 0;
  for (const pat of gene.signals_match) {
    if (matchPatternToSignals(pat, signals)) score += 1;
  }
  return score;
}
```

**淘汰规则**：
- 信号匹配度为0的基因直接淘汰
- 按匹配度排序，只保留前N个候选

---

### 第2层：失败封禁机制（Ban System）

```javascript
var FAILED_CAPSULE_BAN_THRESHOLD = 2;  // 失败2次封禁
var FAILED_CAPSULE_OVERLAP_MIN = 0.6;   // 信号重叠度60%

function banGenesFromFailedCapsules(failedCapsules, signals) {
  // 统计每个基因的失败次数
  // 超过阈值 → 加入封禁列表
}
```

**淘汰规则**：
- 连续失败2次的基因被封禁
- 封禁后该基因不会再被选中

---

### 第3层：风险评估与降级（Mutation）

**位置**：[`mutation.js`](../../evolver/src/gep/mutation.js)

```javascript
// 高风险个性禁止创新
if (base.category === 'innovate' && highRiskPersonality) {
  base.category = 'optimize';  // 降级为优化
  base.risk_level = 'low';
}

// 高风险变异需要严格约束
if (base.risk_level === 'high' && !isHighRiskMutationAllowed(personalityState)) {
  base.risk_level = 'medium';  // 强制降级
}
```

**降级策略**：
| 原始风险 | 条件 | 降级后 |
|---------|------|--------|
| innovate + 高风险个性 | 严谨性<0.5 或 风险容忍度>0.6 | optimize |
| high风险 | 不满足高风险许可条件 | medium |

---

### 第4层：硬约束检查（Solidify）

**位置**：[`solidify.js`](../../evolver/src/gep/solidify.js)

```javascript
// 系统级硬上限
const BLAST_RADIUS_HARD_CAP_FILES = 60;
const BLAST_RADIUS_HARD_CAP_LINES = 20000;

// 伦理审查
var ethicsBlockPatterns = [
  { re: /bypass|disable.*safety/i, msg: '试图绕过安全机制' },
  { re: /keylogger|screen capture/i, msg: '监控工具' },
];
```

**绝对淘汰条件**：
- 变更文件数 > 60
- 变更行数 > 20000
- 修改关键保护路径（如 evolver/ 自身）
- 违反伦理规则（安全绕过、监控等）
- 金丝雀检查失败（index.js无法加载）

---

### 第5层：表观遗传标记（Epigenetic Marks）

**最精妙的机制！**

```javascript
function applyEpigeneticMarks(gene, envFingerprint, outcomeStatus) {
  if (outcomeStatus === 'success') {
    // 成功：增强表达（boost +0.05）
    cur.boost = Math.min(0.5, cur.boost + 0.05);
    cur.reason = 'reinforced_by_success';
  } else if (outcomeStatus === 'failed') {
    // 失败：抑制表达（boost -0.1）
    cur.boost = Math.max(-0.5, cur.boost - 0.1);
    cur.reason = 'suppressed_by_failure';
  }
  
  // 90天后自动衰减
  const cutoff = Date.now() - 90 * 24 * 60 * 60 * 1000;
  gene.epigenetic_marks = gene.epigenetic_marks
    .filter(m => new Date(m.created_at).getTime() > cutoff)
    .slice(-10);  // 最多保留10个
}
```

**优胜劣汰原理**：
- ✅ **成功强化**：在特定环境下成功的基因，下次匹配时得分更高
- ❌ **失败抑制**：失败的基因在该环境下得分降低
- ⏰ **时间衰减**：老旧的标记自动消失，保持基因库新鲜度
- 🌍 **环境适应**：不同环境下基因表现可以不同

**应用示例**：
```javascript
// 在 Windows 环境下成功的修复基因
{
  "id": "gene_windows_path_fix",
  "epigenetic_marks": [
    {
      "context": "win32/x64/v18.17.0",
      "boost": 0.3,  // 在Windows环境下得分+30%
      "reason": "reinforced_by_success"
    }
  ]
}
```

---

### 第6层：胶囊成功率追踪（Success Streak）

```javascript
// 计算连续成功次数
const streak = computeCapsuleSuccessStreak({ capsuleId });

// 只有优质胶囊才能广播到Hub
capsule.a2a.eligible_to_broadcast = 
  isBlastRadiusSafe(capsule.blast_radius) &&
  (capsule.outcome.score || 0) >= 0.7 &&
  (capsule.success_streak || 0) >= 2;
```

**筛选标准**：
- 爆炸半径安全
- 得分 ≥ 0.7
- 连续成功 ≥ 2次

---

## 优胜劣汰流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    基因候选池                                │
│  [gene_1] [gene_2] [gene_3] [gene_4] [gene_5] [gene_6]     │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  第1层：信号匹配筛选                                         │
│     - 匹配度 = 0 → ❌ 淘汰                                   │
│     - 在封禁列表 → ❌ 淘汰                                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  第2层：风险评估与降级                                       │
│     - 高风险个性 + 创新 → ⚠️ 降级为优化                      │
│     - 不满足安全条件 → ⚠️ 降级风险等级                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  第3层：执行与验证                                           │
│     - 硬约束检查 → ❌ 淘汰（文件/行数超限）                  │
│     - 伦理审查 → ❌ 淘汰（安全违规）                         │
│     - 金丝雀检查 → ❌ 淘汰（运行时错误）                     │
│     - 验证命令失败 → ❌ 淘汰                                 │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  第4层：固化结果与表观遗传                                   │
│     ┌─────────────┐    ┌─────────────┐                      │
│     │   成功 ✅   │    │   失败 ❌   │                      │
│     └──────┬──────┘    └──────┬──────┘                      │
│            │                   │                             │
│            ▼                   ▼                             │
│     表观遗传增强          表观遗传抑制                       │
│     (+0.05 boost)         (-0.1 boost)                      │
│     胶囊发布              失败记录                           │
│     知识共享              反模式发布                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 关键代码位置

| 机制 | 文件路径 | 关键函数 |
|------|---------|---------|
| 信号匹配 | `evolver/src/gep/selector.js` | `scoreGene()`, `selectGene()` |
| 失败封禁 | `evolver/src/gep/selector.js` | `banGenesFromFailedCapsules()` |
| 风险评估 | `evolver/src/gep/mutation.js` | `buildMutation()`, `isHighRiskPersonality()` |
| 硬约束检查 | `evolver/src/gep/solidify.js` | `checkConstraints()`, `classifyBlastSeverity()` |
| 伦理审查 | `evolver/src/gep/solidify.js` | `ethicsBlockPatterns` |
| 表观遗传 | `evolver/src/gep/solidify.js` | `applyEpigeneticMarks()`, `getEpigeneticBoost()` |
| 金丝雀检查 | `evolver/src/gep/solidify.js` | `runCanaryCheck()` |
| 成功率追踪 | `evolver/src/gep/solidify.js` | `computeCapsuleSuccessStreak()` |

---

## 实际案例

### 案例1：表观遗传的环境适应

```javascript
// gene_gep_repair_from_errors 在不同环境的表现
{
  "id": "gene_gep_repair_from_errors",
  "epigenetic_marks": [
    {
      "context": "linux/x64/v22.22.0",
      "boost": 0.25,
      "reason": "reinforced_by_success"
    },
    {
      "context": "win32/x64/v18.17.0",
      "boost": -0.1,
      "reason": "suppressed_by_failure"
    }
  ]
}
```

**结果**：在 Linux 环境下该基因得分+25%，在 Windows 环境下得分-10%

### 案例2：失败封禁

```javascript
// 某基因连续2次在相似信号下失败
failedCapsules = [
  { gene: "gene_bad_idea", trigger: ["error", "windows_path"], outcome: "failed" },
  { gene: "gene_bad_idea", trigger: ["error", "windows_shell"], outcome: "failed" }
];

// 信号重叠度 > 60%，触发封禁
bannedGeneIds = { "gene_bad_idea" };
```

**结果**：`gene_bad_idea` 被加入封禁列表，不再被选中

### 案例3：伦理审查拦截

```javascript
// 某个基因的策略包含危险内容
{
  "id": "gene_malicious",
  "strategy": ["bypass the safety check", "hide logs from user"]
}

// 伦理审查匹配到模式
ethicsBlockPatterns[0].re.test(strategy) // true
// → 触发违规：ethics: strategy attempts to bypass safety mechanisms
```

**结果**：该基因被立即淘汰，无法进入执行阶段

---

## 优胜劣汰的效果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 重复失败率 | 高 | 低（失败基因被封禁） |
| 环境适应性 | 差 | 好（表观遗传标记） |
| 解决方案质量 | 不稳定 | 稳定（连续成功筛选） |
| 知识共享效率 | 低 | 高（优质胶囊自动发布） |

---

## 相关话题

- [Gene 与 Capsule](./gene-and-capsule.md) - 理解优胜劣汰的对象
- [匹配机制](./matching-mechanism.md) - 了解第一层筛选的原理
- [Evolver Hub 与 EvoMap 网络](./evolver-hub-and-network.md) - 优质基因如何共享
- [认知偏差预防机制](./cognitive-bias-prevention.md) - 避免选择偏差

---

## 参考链接

- [Selector 实现](../../evolver/src/gep/selector.js)
- [Mutation 实现](../../evolver/src/gep/mutation.js)
- [Solidify 实现](../../evolver/src/gep/solidify.js)
- [EvoMap 官网](https://evomap.ai)

---

*本话题由知识积累基因驱动生成*
*创建时间: 2026-02-25*
